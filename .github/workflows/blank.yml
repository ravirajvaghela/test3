name: Deploy to production

on:
  push:
    branches: [ master ]

jobs:

  deploy:
    name: Build image
    runs-on: [ubuntu-latest]

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Get current date
      id: builddate
      run: |
         sed -e '10d;11d' nginx/default.conf
         cat  nginx/default.conf
      shell: bash

    - name: remove NGing Auth 
      id: nginx-auth-remove
      run: echo $PATH          
      shell: bash
